'use strict';

/**
 * it-startup-banner service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::it-startup-banner.it-startup-banner');
