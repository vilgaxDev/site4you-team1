'use strict';

/**
 * app-minimal-dashboard service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::app-minimal-dashboard.app-minimal-dashboard');
